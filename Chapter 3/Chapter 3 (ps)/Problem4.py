# Replace the double Spaces from Problem 3 with Single spaces

name = "This is  a  string with  double spaces"
print(name.replace("  "," "))
print(name)# String is immutable which means that you cannot change them by running function on them.